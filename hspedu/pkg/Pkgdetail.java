package hspedu.pkg;//import放到类的定义前面，在package下面必须
import java.util.Arrays;
import java.util.Scanner;//可以有多条

public class Pkgdetail {
    public static void main (String[]args){
       Scanner scanner= new Scanner(System.in);
       int []arr={0,-1,1};
        Arrays.sort (args);
    }
}
